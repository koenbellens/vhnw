# Log de live-gebruiker (vhnw) automatisch in op tty1, zodat ~/.bash_profile
# de grafische kiosk (startx -> chromium) kan starten op het laptopscherm zelf.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin vhnw --noclear %I $TERM
